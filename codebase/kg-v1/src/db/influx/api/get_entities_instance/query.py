query = "select * from %s"

#query is the first part of the query that selects the attribute
#measurement where ID =

#query_ is the second part of the query where you mention the ID you are filtering using. 
#currently its bizId but must change to entity_id after identity service